.container.mt-20.p-12.w-50.border.rounded.shadow-sm
  %h2 Forgot your password?
  = simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    = f.error_notification
    .mb-12
      = f.label :email, class: 'form-label'
      = f.text_field :email, required: true, autofocus: true, autocomplete: 'email', class: 'form-control'
    .mb-12.d-flex.align-items-center.justify-content-center
      = f.submit 'Reset Password', class: 'btn btn-lg btn-primary rounded-pill'
